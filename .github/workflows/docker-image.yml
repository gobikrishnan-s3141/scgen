name: scGen CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: scgen
      run: docker build . --file Dockerfile --tag scgen:$(date +%s)
